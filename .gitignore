# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.uv/
uv.lock.tmp

# Test / type / lint caches
.pytest_cache/
.ruff_cache/
.pyright/
.coverage
htmlcov/
.mypy_cache/

# Env / secrets — never commit
.env
.env.*
!.env.example
!.env.local.example
*.pem
*.key
secrets/

# Node / Next.js (frontend/)
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
.turbo/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Local databases / run artefacts
*.sqlite3
*.db
local.db

# Railway
.railway/

# The motion build output IS reviewable source, not build litter: the .riv is what ships and the
# rendered still is how a diagram change shows up in a pull request as something a human can see.
!design/motion/build/

# Throwaway probe scripts, run artefacts and screenshots. These were committed once
# (GRS-0177) and cost the repo 75MB and every ruff run; the reusable parts now live in tools/.
scratch/

# Local gbrain workspace marker — machine-specific, never committed.
.gbrain-source
