# Shift report — 2026-07-13

Autonomous ticket-queue shift on Grassmarket. Worked the Planned backlog in order, one ticket =
one branch = one PR, review before ship, never merged, never committed to `main`.

## Interpretation of the queue

No ticket has Status literally `Open`. The status vocabulary in `docs/tickets/` is
`Planned → In progress → In review`. Mapped "the lowest-numbered Open ticket" onto **`Planned`**
(the ready-to-work backlog state) — consistent with the rule "update Status when the PR opens",
which is exactly the Planned → In review transition. Skipped GRS-0001 (In progress) and GRS-0003
(PAUSED for John's ratification); they are not backlog. Queue therefore started at **GRS-0016**.

## Completed this shift

### GRS-0016 — Value-bridge rendering + Modernisation Roadmap → **PR #16 (open, not merged)**
- Branch `grs-0016-value-bridge-rendering`; ticket Status set to `In review — PR #16` (on the branch).
- New: `deliverables/roadmap.py` (Modernisation Roadmap builder — ranked interventions, three-layer
  value bridge, scenario comparison table, assumption register), `deliverables/charts.py`
  (priority-vs-cost scatter, matplotlib Agg, byte-deterministic PNG), `deliverables/money_text.py`
  (currency display). Extended `deliverables/service.py` with `render_modernisation_roadmap`
  (client-usable gate runs first). New `tests/test_roadmap.py`.
- **Honest by construction (ADR-0002):** ranks by ΔV (score domain), prices via the value bridge
  (currency), never divided into one ROI number. Every intervention cost is a register-linked
  `Money`; traceability enforced at `RoadmapContext` construction (fail loud, §10).
- **Dependency added:** `matplotlib` (Agg backend, offline-safe) for the scatter — the only chart
  lib in the repo; `deliverables/` architecture already anticipated charts.
- **Review:** ran an independent adversarial pass before ship. Six findings, all fixed:
  1. (P2) indicative cost was an untraceable bare int → register-linked `Money` + construction-time
     validation.
  2. (P2) the "never one ratio" test was vacuous (prose-only) → made structural.
  3. (P3) duplicated/divergent money formatting → routed through `format_money`.
  4. (P3) lever-less bridge crashed mid-render → guarded.
  5. (P3) determinism claimed but untested → deterministic PNG + byte-reproducibility test.
  6. (P3) `-£0` display defect → fixed + tested.
- **Gate:** ruff · pyright · pre-commit all green; **236 backend tests pass** (was 225). ADR-0002
  AST guard (extended to `deliverables/`) stays green — no signature mixes `Score` and `Money`.
- **Scope boundary:** builder + service (gate) + fixtures/tests. API wiring of the roadmap into the
  deliverables endpoint is deliberately left to GRS-0018 (diagnostic-pack-complete).

### GRS-0017 — AI first-draft narratives (gated) → **PR #17 (open, not merged)**
- Branch `grs-0017-ai-draft-narratives`; ticket Status set to `In review — PR #17` (on the branch).
- New: `bcap_contracts/narratives.py` (`AINarrative`, proposed→approved|rejected state machine),
  `deliverables/narrative.py` (injectable `NarrativeDrafter` port + deterministic offline
  `TemplateNarrativeDrafter` with versioned templates + `edit_summary`),
  `web/routers/narratives.py` (propose/approve/list), `migrations/0007_ai_narratives.py`,
  `docs/adr/ADR-0009-ai-narrative-proposal-approval.md`, `tests/test_narratives.py`. Extended the
  gate, builder, service, repository, models, deliverables router, app.
- **Runtime guarantee (#8):** a client-facing pack with any unapproved AI section is refused (409);
  approved narratives + trail render in the methods appendix, labelled `AI-DRAFTED`. Seniority gate
  (PRD §5): a junior-tier author needs Consultant-tier sign-off.
- **ADR-0009 decision:** minimal in-repo proposal/approval model + injectable drafter port now;
  ActiveGraph + Claude Agent SDK adapter later. The SDK is deliberately NOT a dependency (no live
  calls in CI). Seniority ordering `VENTURE_ASSOCIATE < ADVISOR < CONSULTANT`.
- **Scoping × seniority (design note):** narratives are owner-scoped, so senior approval of a
  junior's draft runs on the governance-visibility seam (a Consultant-tier ADMIN/committee reviewer,
  tested). General peer delegation is Loop 5.
- **Review:** independent adversarial pass; six findings all fixed (appendix wiring, seniority
  reachability, double-approval→409 / empty-text→422, silent-default removal, run scope-check).
- **Gate:** ruff · pyright · pre-commit (incl. schema parity) green; **244 backend tests pass**.

## Stopped — clean boundary (not blocked)

Two Loop 4 tickets shipped (GRS-0016, GRS-0017), each a complete PR reviewed before ship. Stopped
here at a clean boundary; the last instruction was scoped to GRS-0017 and each remaining ticket is a
large unit best started on an explicit go-ahead. Not blocked; not ambiguous.

## Next: GRS-0018 — Diagnostic pack complete

The remaining Loop 4 ticket. Assembles the full Diagnostic pack (executive summary + the
Platform Power Report + the Modernisation Roadmap from GRS-0016 + the AI narratives from GRS-0017)
and is the natural place to wire the roadmap into the deliverables API (GRS-0016 left that seam
open). Then GRS-0019 (deliverables-frontend) closes Loop 4.

## Remaining Planned queue (in order)

GRS-0017 (ai-draft-narratives), 0018 (diagnostic-pack-complete), 0019 (deliverables-frontend) —
rest of Loop 4; then Loop 5 (0020–0027 governance/workbench) and Loop 6 (0028–0034). GRS-0033
(elicited-coefficients) is flagged as the launch bottleneck.

## Environment notes (non-obvious, for next session)

- **Git identity was unset** in this repo; configured locally to
  `John Gallagher <john.gallagher@wealthcx.com>`.
- **`python` is not on PATH** — use `uv run python`.
- Two pre-existing, unrelated working-tree changes were left **untouched** and out of every commit:
  modified `.gitignore` and `CLAUDE.md`, plus an untracked junk file `_capability_check_28621.md`
  (a gbrain test page containing the word `ping`). These are not part of any ticket; recommend the
  owner review/discard them separately.
- Full suite runtime ≈ 80s (Monte Carlo tests dominate).
