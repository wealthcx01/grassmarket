{
  "$defs": {
    "CommissionKind": {
      "enum": [
        "engagement",
        "workshop_recovery_fee",
        "retainer"
      ],
      "title": "CommissionKind",
      "type": "string"
    },
    "ConsultantTier": {
      "description": "Advisory Network tier (PRD §2).",
      "enum": [
        "venture_associate",
        "advisor",
        "consultant"
      ],
      "title": "ConsultantTier",
      "type": "string"
    },
    "Currency": {
      "enum": [
        "GBP",
        "USD",
        "EUR"
      ],
      "title": "Currency",
      "type": "string"
    },
    "Money": {
      "additionalProperties": false,
      "description": "A currency amount that cannot exist without an explicit currency and a reference to the\nassumption register that justifies it (Methodology §10, ADR-0002 compliance test).\n\nA lever NPV or remediation cost is only meaningful under stated assumptions; a `Money`\nwithout an ``assumption_register_ref`` is not constructible.",
      "properties": {
        "amount_minor": {
          "description": "Amount in integer minor units (e.g. pence).",
          "title": "Amount Minor",
          "type": "integer"
        },
        "assumption_register_ref": {
          "description": "Reference to the assumption-register entry that justifies this figure. Mandatory: currency claims are never made without stated assumptions (Methodology §10).",
          "minLength": 1,
          "title": "Assumption Register Ref",
          "type": "string"
        },
        "currency": {
          "$ref": "#/$defs/Currency"
        }
      },
      "required": [
        "amount_minor",
        "currency",
        "assumption_register_ref"
      ],
      "title": "Money",
      "type": "object"
    },
    "PaymentStatus": {
      "description": "Lifecycle of a commission line: earned (pending) → invoiced to Bruntsfield → paid out.",
      "enum": [
        "pending",
        "invoiced",
        "paid"
      ],
      "title": "PaymentStatus",
      "type": "string"
    },
    "SourcingAttribution": {
      "description": "Who sourced the deal — the second axis (with tier) of the commission rate (PRD §7).",
      "enum": [
        "self_sourced",
        "bruntsfield_sourced",
        "co_sourced"
      ],
      "title": "SourcingAttribution",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "One earned commission — immutable once recorded. For an engagement commission the provenance\n(tier, attribution, rate_ref, base_value) is stamped at record time, so a later rate change is\nnever retroactive; the line reproduces from its own fields. `payment_status` is the one mutable\nfield (its lifecycle), and it moves forward only (pending → invoiced → paid).",
  "properties": {
    "amount": {
      "$ref": "#/$defs/Money",
      "description": "Computed commission amount (currency; assumptions attached)"
    },
    "attribution": {
      "anyOf": [
        {
          "$ref": "#/$defs/SourcingAttribution"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "base_value": {
      "anyOf": [
        {
          "$ref": "#/$defs/Money"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The contract value the rate was applied to."
    },
    "content_hash": {
      "description": "SHA-256 immutability seal over the line.",
      "minLength": 1,
      "title": "Content Hash",
      "type": "string"
    },
    "created_at": {
      "format": "date-time",
      "title": "Created At",
      "type": "string"
    },
    "earned_on": {
      "anyOf": [
        {
          "format": "date",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Earned On"
    },
    "engagement_id": {
      "anyOf": [
        {
          "format": "uuid",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Engagement Id"
    },
    "id": {
      "format": "uuid",
      "title": "Id",
      "type": "string"
    },
    "kind": {
      "$ref": "#/$defs/CommissionKind"
    },
    "owner_consultant_id": {
      "description": "The consultant who owns this resource. Data scoping is by this field, enforced in the repository layer and tested from day one.",
      "format": "uuid",
      "title": "Owner Consultant Id",
      "type": "string"
    },
    "payment_status": {
      "$ref": "#/$defs/PaymentStatus",
      "default": "pending"
    },
    "rate_ref": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Rate Ref"
    },
    "source_attribution_id": {
      "anyOf": [
        {
          "format": "uuid",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The recovery-fee attribution this line was claimed from, if any.",
      "title": "Source Attribution Id"
    },
    "tier": {
      "anyOf": [
        {
          "$ref": "#/$defs/ConsultantTier"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "updated_at": {
      "format": "date-time",
      "title": "Updated At",
      "type": "string"
    }
  },
  "required": [
    "id",
    "created_at",
    "updated_at",
    "owner_consultant_id",
    "kind",
    "amount",
    "content_hash"
  ],
  "title": "CommissionLine",
  "type": "object"
}
