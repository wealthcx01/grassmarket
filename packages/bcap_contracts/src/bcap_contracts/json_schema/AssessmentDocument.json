{
  "$defs": {
    "BusinessProfile": {
      "additionalProperties": false,
      "description": "Descriptive context for the business being assessed (GRS-0068): where it operates, what it\noffers, and how it's regulated. Most fields are purely informational (never a scoring input,\nin the content hash). The one exception is `operating_model` (GRS-0079): the assessment PROFILE\nkey (retail / exchange / …), which selects the registry view + coefficient set the assessment\nscores against (ADR-0025). It is not a *value* the engine reads, but it does decide WHICH keys\nand weights apply — so it is scoring-relevant config, not free-text. `segment` stays the\nfree-text descriptor. Partial by design: every field is optional; `operating_model` unset means\nthe retail default (byte-identical to v1).",
      "properties": {
        "asset_classes": {
          "default": [],
          "description": "Asset classes offered (equities, funds, FX, crypto, …).",
          "items": {
            "type": "string"
          },
          "title": "Asset Classes",
          "type": "array"
        },
        "country": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Primary domicile / HQ jurisdiction.",
          "title": "Country"
        },
        "licensing": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Regulatory status / key licences (free text).",
          "title": "Licensing"
        },
        "operating_model": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Operating-model profile key: 'retail' (default), 'exchange', …",
          "title": "Operating Model"
        },
        "regions": {
          "default": [],
          "description": "Markets / regions served.",
          "items": {
            "type": "string"
          },
          "title": "Regions",
          "type": "array"
        },
        "segment": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Free-text business descriptor, e.g. 'Neobroker'.",
          "title": "Segment"
        }
      },
      "title": "BusinessProfile",
      "type": "object"
    },
    "EvidenceGrade": {
      "description": "Evidence-strength grade (Methodology §3.3). Drives uncertainty (§7), not the point score.",
      "enum": [
        "E1",
        "E2",
        "E3",
        "E4"
      ],
      "title": "EvidenceGrade",
      "type": "string"
    },
    "MaturityLevel": {
      "description": "The four maturity levels (Methodology §3.1).\n\nThe numeric ``index`` is for computation only; clients see the label. These four indices\nare the ONLY legal subcomponent indices — any other value is a contract violation, not a\ndatum to clamp or round (ADR-0001 §1).",
      "enum": [
        "Basic",
        "Developing",
        "Advanced",
        "Frontier"
      ],
      "title": "MaturityLevel",
      "type": "string"
    },
    "MetricConfidence": {
      "description": "Source/recency grade for a business-metric observation (Methodology v1.2 §7, ADR-0008).\n\nDrives the Monte Carlo distribution width on a metric's raw value the way an evidence grade\ndrives a subcomponent's — a coarse read of how much the reported number could move under a\nbetter source or fresher data. Audited/current is tight; an estimate is wide. Reported as a\ngrade, never a decimal (the width is a coefficient, ADR-0008).",
      "enum": [
        "estimated",
        "self_reported",
        "management_reported",
        "audited"
      ],
      "title": "MetricConfidence",
      "type": "string"
    },
    "MetricEntry": {
      "additionalProperties": false,
      "description": "A business-metric entry in the intermediate document: a raw value in the declared unit OR a\nnon-score state, with an OPTIONAL source/recency confidence (drives §7 uncertainty, ADR-0008).",
      "properties": {
        "confidence": {
          "anyOf": [
            {
              "$ref": "#/$defs/MetricConfidence"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "metric_key": {
          "title": "Metric Key",
          "type": "string"
        },
        "raw": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Raw"
        },
        "state": {
          "anyOf": [
            {
              "$ref": "#/$defs/NonScoreState"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        }
      },
      "required": [
        "metric_key"
      ],
      "title": "MetricEntry",
      "type": "object"
    },
    "NonScoreState": {
      "description": "First-class non-score states (Methodology §3.2). Never imputed, never scored as zero.\n\nAn unassessed subcomponent contributes to NO score — the empty-module → q_m = 0.0\nbehaviour of the prototype (defect D9) is prohibited by construction.",
      "enum": [
        "Not Applicable",
        "Not Assessed"
      ],
      "title": "NonScoreState",
      "type": "string"
    },
    "PowerEntry": {
      "additionalProperties": false,
      "description": "A power entry: dual Benefit + Barrier strengths (Helmer, §8), each with OPTIONAL evidence\ngrade (drives §7 uncertainty, ADR-0008) and evidence text. Powers are never N/A (§8).",
      "properties": {
        "barrier": {
          "$ref": "#/$defs/StrengthRating"
        },
        "barrier_evidence": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Barrier Evidence"
        },
        "barrier_grade": {
          "anyOf": [
            {
              "$ref": "#/$defs/EvidenceGrade"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "benefit": {
          "$ref": "#/$defs/StrengthRating"
        },
        "benefit_evidence": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Benefit Evidence"
        },
        "benefit_grade": {
          "anyOf": [
            {
              "$ref": "#/$defs/EvidenceGrade"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "power_key": {
          "title": "Power Key",
          "type": "string"
        },
        "trend": {
          "anyOf": [
            {
              "$ref": "#/$defs/TrendDirection"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        }
      },
      "required": [
        "power_key",
        "benefit",
        "barrier"
      ],
      "title": "PowerEntry",
      "type": "object"
    },
    "StrengthRating": {
      "description": "Power strength and triad rating — ordinal with falsifiable duration semantics (§8).\n\nReported as a rating, NEVER as a decimal. Establishing this as an enum (not a float) is the\nADR-0002 guarantee that no decimal leaks into a triad figure.",
      "enum": [
        "None",
        "Emerging",
        "Established",
        "Wide"
      ],
      "title": "StrengthRating",
      "type": "string"
    },
    "SubcomponentRating": {
      "additionalProperties": false,
      "description": "One subcomponent's rating: a maturity level OR a first-class non-score state, never both.\n\nModelling this as an exclusive union is how \"Not Assessed\" stays distinct from a score of\nzero (defect D9) — an unassessed subcomponent carries `state`, not `level`, and contributes\nto no computation.",
      "properties": {
        "consensus": {
          "default": false,
          "title": "Consensus",
          "type": "boolean"
        },
        "dissent_note": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Dissent Note"
        },
        "evidence_grade": {
          "anyOf": [
            {
              "$ref": "#/$defs/EvidenceGrade"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "evidence_refs": {
          "default": [],
          "items": {
            "type": "string"
          },
          "title": "Evidence Refs",
          "type": "array"
        },
        "level": {
          "anyOf": [
            {
              "$ref": "#/$defs/MaturityLevel"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "module_key": {
          "title": "Module Key",
          "type": "string"
        },
        "notes": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Notes"
        },
        "rater_ids": {
          "default": [],
          "items": {
            "format": "uuid",
            "type": "string"
          },
          "title": "Rater Ids",
          "type": "array"
        },
        "state": {
          "anyOf": [
            {
              "$ref": "#/$defs/NonScoreState"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "subcomponent_key": {
          "title": "Subcomponent Key",
          "type": "string"
        }
      },
      "required": [
        "module_key",
        "subcomponent_key"
      ],
      "title": "SubcomponentRating",
      "type": "object"
    },
    "TrendDirection": {
      "enum": [
        "improving",
        "stable",
        "eroding"
      ],
      "title": "TrendDirection",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "The single intermediate schema BOTH Path A (wizard) and Path B (meeting intelligence) feed.\n\nIt is **partial by design**: a half-filled document is valid and persistable (autosave). It is\nNOT the engine input — the engine requires exact registry coverage; the live-score service\ncompletes the missing subcomponents/metrics to Not Assessed (first-class, never zero-filled).",
  "properties": {
    "metrics": {
      "default": [],
      "items": {
        "$ref": "#/$defs/MetricEntry"
      },
      "title": "Metrics",
      "type": "array"
    },
    "notes": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Notes"
    },
    "powers": {
      "default": [],
      "items": {
        "$ref": "#/$defs/PowerEntry"
      },
      "title": "Powers",
      "type": "array"
    },
    "profile": {
      "anyOf": [
        {
          "$ref": "#/$defs/BusinessProfile"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "subcomponents": {
      "default": [],
      "items": {
        "$ref": "#/$defs/SubcomponentRating"
      },
      "title": "Subcomponents",
      "type": "array"
    },
    "subject": {
      "default": "",
      "title": "Subject",
      "type": "string"
    }
  },
  "title": "AssessmentDocument",
  "type": "object"
}
