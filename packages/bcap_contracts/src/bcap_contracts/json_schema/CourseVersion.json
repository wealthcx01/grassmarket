{
  "$defs": {
    "CertificationCredit": {
      "description": "What completing a module grants toward certification evidence (§9). Only COURSEWORK is\nself-service — it is binary and platform-verifiable (\"did the module\"). An EXAM score is\nobjective, so it is never self-attested: the certification exam is PROCTORED and admin-recorded\n(GRS-0023). An exam-quiz here is practice content, not a certification credit (ADR-0014).",
      "enum": [
        "none",
        "coursework"
      ],
      "title": "CertificationCredit",
      "type": "string"
    },
    "CourseModule": {
      "additionalProperties": false,
      "description": "An ordered group of lessons within a course — a SECTION, in the founder's language.\n\n`section_test` is the gate: the learner passes it before the next section opens (GRS-0215).\nOptional at the contract so a legacy course still validates, required by the depth tests for\nany course claiming to meet the standard.",
      "properties": {
        "id": {
          "format": "uuid",
          "title": "Id",
          "type": "string"
        },
        "lessons": {
          "default": [],
          "items": {
            "$ref": "#/$defs/Lesson"
          },
          "title": "Lessons",
          "type": "array"
        },
        "order": {
          "minimum": 0,
          "title": "Order",
          "type": "integer"
        },
        "section_test": {
          "anyOf": [
            {
              "$ref": "#/$defs/SectionTest"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "title": {
          "minLength": 1,
          "title": "Title",
          "type": "string"
        }
      },
      "required": [
        "id",
        "title",
        "order"
      ],
      "title": "CourseModule",
      "type": "object"
    },
    "CourseTree": {
      "additionalProperties": false,
      "description": "The editable body of a course — metadata plus its nested modules/lessons. This is what an\nadmin edits in the CMS; publishing snapshots it into an immutable `CourseVersion`.",
      "properties": {
        "certification_credit": {
          "$ref": "#/$defs/CertificationCredit",
          "default": "none"
        },
        "mandatory_first": {
          "default": false,
          "description": "A new advisor's learning path opens on this course (GRS-0122). At most one course should carry the flag; the catalog surfaces it first.",
          "title": "Mandatory First",
          "type": "boolean"
        },
        "modules": {
          "default": [],
          "items": {
            "$ref": "#/$defs/CourseModule"
          },
          "title": "Modules",
          "type": "array"
        },
        "summary": {
          "minLength": 1,
          "title": "Summary",
          "type": "string"
        },
        "title": {
          "minLength": 1,
          "title": "Title",
          "type": "string"
        }
      },
      "required": [
        "title",
        "summary"
      ],
      "title": "CourseTree",
      "type": "object"
    },
    "Lesson": {
      "additionalProperties": false,
      "description": "One teaching unit inside a module: a markdown body, an optional video, and links to the\nspaced-repetition drill topics it reinforces. An AI-authored lesson (`author == AI`) is not\n``approved`` until a human signs off — the approver is then recorded (ADR-0009).\n\n`slides` is where the teaching now lives (GRS-0215). `body` remains the lesson's opening — what\nthis lesson is for and what the advisor will be able to do at the end — and is no longer\nexpected to carry the content on its own. Existing courses with no slides still validate; the\ndepth tests, not the contract, are what refuse a thin course, so a legacy course can be read\nwhile it waits to be rebuilt.",
      "properties": {
        "approved": {
          "default": true,
          "title": "Approved",
          "type": "boolean"
        },
        "approved_at": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Approved At"
        },
        "approved_by_consultant_id": {
          "anyOf": [
            {
              "format": "uuid",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Approved By Consultant Id"
        },
        "assets": {
          "default": [],
          "description": "Inline SVG diagrams rendered with the lesson body (GRS-0190).",
          "items": {
            "$ref": "#/$defs/LessonAsset"
          },
          "title": "Assets",
          "type": "array"
        },
        "author": {
          "$ref": "#/$defs/LessonAuthor",
          "default": "human"
        },
        "body": {
          "description": "Lesson content, markdown.",
          "minLength": 1,
          "title": "Body",
          "type": "string"
        },
        "check_answer": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The model answer revealed after the advisor attempts the check (GRS-0139). None → the reader falls back to `measurement` as the answer key.",
          "title": "Check Answer"
        },
        "check_question": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "A retrieval-practice question the advisor answers to complete the lesson (GRS-0139). None → the reader derives a recall prompt from `measurement`.",
          "title": "Check Question"
        },
        "drill_topics": {
          "default": [],
          "description": "Existing DrillCard topics this lesson reinforces (e.g. 'power:…').",
          "items": {
            "type": "string"
          },
          "title": "Drill Topics",
          "type": "array"
        },
        "id": {
          "format": "uuid",
          "title": "Id",
          "type": "string"
        },
        "measurement": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "How the advisor measures they have applied this lesson (GRS-0122).",
          "title": "Measurement"
        },
        "order": {
          "minimum": 0,
          "title": "Order",
          "type": "integer"
        },
        "references": {
          "default": [],
          "description": "Cited public sources, rendered as link cards (GRS-0190).",
          "items": {
            "$ref": "#/$defs/SourceRef"
          },
          "title": "References",
          "type": "array"
        },
        "slides": {
          "default": [],
          "description": "The lesson's slides, in order (GRS-0215).",
          "items": {
            "$ref": "#/$defs/Slide"
          },
          "title": "Slides",
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "title": "Title",
          "type": "string"
        },
        "video_ref": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Video Ref"
        }
      },
      "required": [
        "id",
        "title",
        "body",
        "order"
      ],
      "title": "Lesson",
      "type": "object"
    },
    "LessonAsset": {
      "additionalProperties": false,
      "description": "An interpretive diagram on a lesson (GRS-0190).\n\nAssets are inline SVG strings held in the content itself, not files. A published\n`CourseVersion` is an immutable snapshot, and a snapshot that pointed at external asset storage\nwould not actually be immutable: the file behind it could change or vanish. Raster images are\ndeliberately out of contract; a photograph belongs behind a `SourceRef`.",
      "properties": {
        "alt": {
          "description": "Text alternative. Required, never derived.",
          "minLength": 1,
          "title": "Alt",
          "type": "string"
        },
        "caption": {
          "minLength": 1,
          "title": "Caption",
          "type": "string"
        },
        "svg": {
          "minLength": 1,
          "title": "Svg",
          "type": "string"
        }
      },
      "required": [
        "caption",
        "alt",
        "svg"
      ],
      "title": "LessonAsset",
      "type": "object"
    },
    "LessonAuthor": {
      "description": "Who wrote a lesson. AI-authored content is approval-gated before it can be published (#8).",
      "enum": [
        "human",
        "ai"
      ],
      "title": "LessonAuthor",
      "type": "string"
    },
    "SectionTest": {
      "additionalProperties": false,
      "description": "The test a learner passes before the next section opens (GRS-0215).\n\n`pass_mark` is a fraction of questions correct. It is declared per test rather than fixed\nglobally so a heavier section can demand more, and it is validated here rather than assumed by\nthe marker.",
      "properties": {
        "pass_mark": {
          "default": 0.8,
          "exclusiveMinimum": 0.0,
          "maximum": 1.0,
          "title": "Pass Mark",
          "type": "number"
        },
        "questions": {
          "items": {
            "$ref": "#/$defs/TestQuestion"
          },
          "minItems": 1,
          "title": "Questions",
          "type": "array"
        }
      },
      "required": [
        "questions"
      ],
      "title": "SectionTest",
      "type": "object"
    },
    "Slide": {
      "additionalProperties": false,
      "description": "One slide of a lesson (GRS-0215).\n\nThe founder's standard, in their words: \"A lesson is 20-40 slides of interactive detail with a\ntest before the next section.\" A slide is the unit of that. It is deliberately small — one\nidea, one step, or one worked example — because the failure being corrected was lessons that\nwere a paragraph pretending to be a lesson.\n\n`body` is markdown. `asset` is an inline SVG, for the same immutability reason `LessonAsset`\nis: a published version that pointed at external storage would not actually be published.",
      "properties": {
        "asset": {
          "anyOf": [
            {
              "$ref": "#/$defs/LessonAsset"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "body": {
          "description": "Slide content, markdown.",
          "minLength": 1,
          "title": "Body",
          "type": "string"
        },
        "checkpoint_prompt": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "What the advisor must actually do and confirm (CHECKPOINT slides only).",
          "title": "Checkpoint Prompt"
        },
        "kind": {
          "$ref": "#/$defs/SlideKind",
          "default": "concept"
        },
        "order": {
          "minimum": 0,
          "title": "Order",
          "type": "integer"
        },
        "references": {
          "default": [],
          "description": "Sources for THIS slide. A claim with no source does not belong on a slide.",
          "items": {
            "$ref": "#/$defs/SourceRef"
          },
          "title": "References",
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "title": "Title",
          "type": "string"
        }
      },
      "required": [
        "order",
        "title",
        "body"
      ],
      "title": "Slide",
      "type": "object"
    },
    "SlideKind": {
      "description": "What a slide is for. The renderer styles by this, and the depth tests count by it: a lesson\nthat is 30 slides of prose and no doing is not the lesson the founder asked for.",
      "enum": [
        "concept",
        "walkthrough",
        "example",
        "checkpoint"
      ],
      "title": "SlideKind",
      "type": "string"
    },
    "SourceRef": {
      "additionalProperties": false,
      "description": "A cited public source on a lesson (GRS-0190).\n\n`url` is https-only at the CONTRACT, not merely in the renderer: schema validation refuses\n`http://` and non-URL strings, so a downgraded or javascript: link cannot reach a published\ncourse version in the first place. The renderer's own check is then a second line rather than\nthe only one.",
      "properties": {
        "kind": {
          "$ref": "#/$defs/SourceRefKind"
        },
        "title": {
          "minLength": 1,
          "title": "Title",
          "type": "string"
        },
        "url": {
          "pattern": "^https://",
          "title": "Url",
          "type": "string"
        }
      },
      "required": [
        "title",
        "url",
        "kind"
      ],
      "title": "SourceRef",
      "type": "object"
    },
    "SourceRefKind": {
      "description": "What kind of thing a lesson reference points at, so the reader can label the link card.",
      "enum": [
        "docs",
        "video",
        "blog",
        "repo"
      ],
      "title": "SourceRefKind",
      "type": "string"
    },
    "TestQuestion": {
      "additionalProperties": false,
      "description": "One question on a section test (GRS-0215). Multiple choice with exactly one right answer:\na section gate has to be markable without a human in the loop, and a free-text answer is not.",
      "properties": {
        "answer_index": {
          "minimum": 0,
          "title": "Answer Index",
          "type": "integer"
        },
        "explanation": {
          "minLength": 1,
          "title": "Explanation",
          "type": "string"
        },
        "options": {
          "items": {
            "type": "string"
          },
          "minItems": 2,
          "title": "Options",
          "type": "array"
        },
        "prompt": {
          "minLength": 1,
          "title": "Prompt",
          "type": "string"
        }
      },
      "required": [
        "prompt",
        "options",
        "answer_index",
        "explanation"
      ],
      "title": "TestQuestion",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "An immutable, published snapshot of a course tree (append-only; retained forever).",
  "properties": {
    "course_id": {
      "format": "uuid",
      "title": "Course Id",
      "type": "string"
    },
    "published_at": {
      "format": "date-time",
      "title": "Published At",
      "type": "string"
    },
    "published_by_consultant_id": {
      "format": "uuid",
      "title": "Published By Consultant Id",
      "type": "string"
    },
    "slug": {
      "minLength": 1,
      "title": "Slug",
      "type": "string"
    },
    "tree": {
      "$ref": "#/$defs/CourseTree"
    },
    "version": {
      "minimum": 1,
      "title": "Version",
      "type": "integer"
    }
  },
  "required": [
    "course_id",
    "slug",
    "version",
    "tree",
    "published_by_consultant_id",
    "published_at"
  ],
  "title": "CourseVersion",
  "type": "object"
}
